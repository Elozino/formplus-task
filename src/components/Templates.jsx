import React from "react";
import TemplateCard from "./TemplateCard";

function Templates({ data, index }) {
  // console.log(data);
  return (
    <div className="flex">
      {/* <TemplateCard data={data} key={index} /> */}
    </div>
  );
}

export default Templates;
